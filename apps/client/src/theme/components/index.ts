// Combine all component style configs
import { button } from "./button";
import { input } from "./input";
import { link } from "./link";

export const components = {
	Button: button,
	Input: input,
	Link: link,
};
